		//<![CDATA[
			if(window.addEventListener) {
				// Non-IE
				window.addEventListener("resize", swapBanner, false);
			} else {
				// IE
				window.attachEvent("onresize", swapBanner);
			}
		//]]>	